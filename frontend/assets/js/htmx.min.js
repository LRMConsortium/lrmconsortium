/* PLACEHOLDER — HTMX is loaded from CDN by the LRMC layouts.
 *
 * To self-host (recommended for production, required for offline use in the
 * Accra office), replace this file with the real one:
 *
 *   curl -o assets/js/htmx.min.js https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js
 *
 * then in each layout swap the CDN <script> for the commented local line
 * directly beneath it.
 */
