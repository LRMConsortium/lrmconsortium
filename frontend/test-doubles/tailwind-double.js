/* TEST DOUBLE — Tailwind stand-in. Accepts the config call and does nothing;
 * layout classes are not simulated, so this harness tests behaviour and
 * content, not visual layout. Layout is checked by screenshot separately. */
window.tailwind = {};
